---
phase: 03-vertraege-entdecken
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/components/vertraege/VertraegeKachel.tsx
  - src/components/vertraege/FilterPillRow.tsx
  - src/components/vertraege/ProviderCard.tsx
  - src/app/(main)/vertraege/page.tsx
  - src/app/(main)/vertraege/strom/page.tsx
  - src/app/(main)/vertraege/internet/page.tsx
  - src/app/(main)/vertraege/telefon/page.tsx
  - src/app/(main)/vertraege/versicherungen/page.tsx
autonomous: true
requirements:
  - GTK-01
must_haves:
  truths:
    - "User can open Verträge tab and see 4 product kacheln (Strom, Internet, Telefon, Versicherungen) in 2x2 grid"
    - "Verträge hub shows completion progress 'X von 4 Verträgen abgeschlossen' driven by vertraegeStore"
    - "Tapping a kachel navigates to /vertraege/{kategorie}"
    - "Each comparison screen shows sticky header with title + PLZ subline + filter pill row + 4 provider cards"
    - "Versicherungen screen has Haftpflicht/Hausrat tabs (D-01) with separate provider lists"
    - "Each provider card shows logo placeholder, name, rating, price (28px), duration, highlight tags, badges (Empfohlen/Partnerangebot)"
    - "Completed kacheln show green ring (#22c55e) and CheckCircle2 icon (D-05)"
  artifacts:
    - path: "src/app/(main)/vertraege/page.tsx"
      provides: "Verträge Hub page (replaces placeholder)"
      contains: "VertraegeKachel"
    - path: "src/app/(main)/vertraege/strom/page.tsx"
      provides: "Strom comparison screen"
      contains: "ProviderCard"
    - path: "src/app/(main)/vertraege/internet/page.tsx"
      provides: "Internet comparison screen"
      contains: "ProviderCard"
    - path: "src/app/(main)/vertraege/telefon/page.tsx"
      provides: "Telefon comparison screen"
      contains: "ProviderCard"
    - path: "src/app/(main)/vertraege/versicherungen/page.tsx"
      provides: "Versicherungen comparison with Haftpflicht/Hausrat tabs"
      contains: "Tabs"
    - path: "src/components/vertraege/VertraegeKachel.tsx"
      provides: "2x2 grid kachel with completed-state ring"
      contains: "ring-2"
    - path: "src/components/vertraege/FilterPillRow.tsx"
      provides: "Empfohlen/Günstigste/Beliebteste filter pills"
      contains: "Empfohlen"
    - path: "src/components/vertraege/ProviderCard.tsx"
      provides: "Provider comparison card"
      contains: "€/Monat"
  key_links:
    - from: "src/app/(main)/vertraege/page.tsx"
      to: "src/store/vertraegeStore.ts"
      via: "useVertraegeStore hook"
      pattern: "useVertraegeStore"
    - from: "src/app/(main)/vertraege/strom/page.tsx"
      to: "src/lib/anbieter.ts"
      via: "getAnbieterByKategorie('strom')"
      pattern: "getAnbieterByKategorie"
    - from: "src/app/(main)/vertraege/strom/page.tsx"
      to: "src/store/onboardingStore.ts"
      via: "data.targetPlz for header subline"
      pattern: "targetPlz"
---

<objective>
Build the Verträge tab end-to-end: hub with 4 kacheln + 4 comparison screens (Strom, Internet, Telefon, Versicherungen with Haftpflicht/Hausrat tabs). Vertical slice — user can navigate hub → comparison → see provider cards. Abschluss flow comes in plan 03 (depends on this plan's ProviderCard component).

Purpose: Deliver the monetization tab as a complete, navigable feature. Hub progress driven by vertraegeStore, PLZ subline driven by onboardingStore.

Output: 3 new components + 5 page files (1 placeholder replaced, 4 new sub-routes).
</objective>

<execution_context>
@C:/Users/JilSchmidt/Projects/Wone MOVE/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JilSchmidt/Projects/Wone MOVE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-vertraege-entdecken/03-UI-SPEC.md
@.planning/phases/03-vertraege-entdecken/03-PATTERNS.md
@.planning/phases/03-vertraege-entdecken/03-CONTEXT.md
@src/app/(main)/vertraege/page.tsx
@src/app/(main)/anleitungen/page.tsx
@src/app/(main)/aufgaben/uebergabeprotokoll/page.tsx
@src/app/(main)/home/kostenrechner/page.tsx
@src/components/ui/card.tsx
@src/components/ui/badge.tsx
@src/components/ui/tabs.tsx

<interfaces>
From plan 03-01:
- `VertragKategorie = 'strom' | 'internet' | 'telefon' | 'haftpflicht' | 'hausrat'`
- `Anbieter { id, kategorie, name, preisProMonat, laufzeit, rating, highlights, empfohlen?, partnerangebot? }`
- `ANBIETER`, `getAnbieterByKategorie(k)`, `KATEGORIE_META` from `@/lib/anbieter`
- `useVertraegeStore` with `completed`, `markComplete(k)`, `completedCount()` — 4 hub keys: strom/internet/telefon/versicherungen
- `useOnboardingStore` with `data.targetPlz`

Tabs (Base UI): use `data-active:*` selectors, NOT `data-[state=active]:*`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build 3 Verträge components (VertraegeKachel, FilterPillRow, ProviderCard)</name>
  <files>src/components/vertraege/VertraegeKachel.tsx, src/components/vertraege/FilterPillRow.tsx, src/components/vertraege/ProviderCard.tsx</files>
  <read_first>
    - .planning/phases/03-vertraege-entdecken/03-UI-SPEC.md (sections 1, 2 for kachel/filter/provider card specs)
    - .planning/phases/03-vertraege-entdecken/03-PATTERNS.md (ProviderCard, FilterPillRow, VertraegeKachel sections)
    - src/components/checklist/AddItemSheet.tsx (FilterPillRow analog — category pills)
    - src/components/ui/card.tsx
    - src/app/(main)/anleitungen/page.tsx (Card+Link pattern for kachel)
  </read_first>
  <action>
**File 1: src/components/vertraege/VertraegeKachel.tsx**

`'use client'` at top. Props: `{ kategorie: 'strom'|'internet'|'telefon'|'versicherungen'; emoji: string; label: string; completed: boolean }`.

Wrap with `<Link href={\`/vertraege/${kategorie}\`}>`. Card: `flex h-[88px] flex-col items-center justify-center gap-1 rounded-[14px] bg-white p-3 transition-transform active:scale-[0.97]`.

Border: when `completed`, `border-2 border-[#22c55e]` (green ring per UI-SPEC §1 + D-05); otherwise `border-[1.5px] border-[#d2d5fc]`.

Layout inside: emoji span (text-[24px]) centered top, label `text-[14px] font-bold text-foreground` centered, when completed render `<CheckCircle2 size={16} className="absolute right-2 bottom-2 text-[#22c55e]" />` (use Lucide). Card needs `relative` for absolute positioning of the check.

**File 2: src/components/vertraege/FilterPillRow.tsx**

`'use client'`. Props: `{ active: 'empfohlen'|'guenstigste'|'beliebteste'; onChange: (v) => void }`.

Three pills in a row: container `flex gap-2 overflow-x-auto pb-1`.

Each pill: button with `h-8 shrink-0 rounded-full px-3 text-[14px] font-bold transition-colors`. Active pill: `bg-primary text-white`. Inactive: `border border-[#d2d5fc] bg-white text-muted-foreground`.

Labels (verbatim per UI-SPEC §2): "Empfohlen", "Günstigste", "Beliebteste".

Default active is set by parent (`'empfohlen'`). This component is presentation-only; sorting logic stays in parent (for prototype, sorting is visual-only — no real sort).

**File 3: src/components/vertraege/ProviderCard.tsx**

`'use client'`. Props: `{ anbieter: Anbieter; onAbschliessen: () => void }`.

Card outer: `relative rounded-[14px] border border-[#d2d5fc] bg-white p-4`.

Top row (flex items-center gap-3):
- Logo placeholder: `flex h-10 w-10 shrink-0 items-center justify-center rounded-lg border border-[#d2d5fc] bg-background text-[14px] font-bold text-muted-foreground` showing first 2 letters of `anbieter.name`.
- Name: `text-[16px] font-bold text-foreground` (flex-1)
- Rating: render 5 spans of `★`/`☆` (filled for `i < anbieter.rating`), `text-[14px]`, filled color `#f59e0b`, empty `#d2d5fc`.

Price row (flex items-baseline gap-2 mt-3):
- Number: `<span className="text-[28px] font-bold leading-[1.2] text-foreground">{anbieter.preisProMonat} €</span>`
- Unit: `<span className="text-[16px] font-normal text-muted-foreground">/Monat</span>`

Duration: `<p className="mt-1 text-[14px] font-normal text-muted-foreground">{anbieter.laufzeit} Laufzeit</p>` — but if `laufzeit === 'Keine Laufzeit'` render just "Keine Mindestlaufzeit".

Highlight tags (mt-3 flex flex-wrap gap-1):
```
<span className="rounded-full border border-[#d2d5fc] bg-background px-2 py-1 text-[14px] font-bold text-foreground">{tag}</span>
```

Badges (positioned top-right via absolute; mutually exclusive — Empfohlen wins):
- If `empfohlen`: `<span className="absolute right-3 top-3 rounded-full bg-primary px-2 py-1 text-[14px] font-bold text-white">Empfohlen</span>`
- Else if `partnerangebot`: `<span className="absolute right-3 top-3 rounded-full bg-[#d2d5fc] px-2 py-1 text-[14px] font-bold text-muted-foreground">Partnerangebot</span>`

CTA button (mt-4):
```
<button type="button" onClick={onAbschliessen}
  className="h-[44px] w-full rounded-[10px] bg-primary text-[14px] font-bold text-white transition-all duration-100 active:scale-[0.97]">
  Jetzt abschließen →
</button>
```
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "ring\|border-2 border-\[#22c55e\]" src/components/vertraege/VertraegeKachel.tsx && grep -c "Empfohlen" src/components/vertraege/FilterPillRow.tsx && grep -c "€/Monat\|/Monat" src/components/vertraege/ProviderCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files have `'use client'` directive at top (grep matches)
    - VertraegeKachel uses `border-2 border-[#22c55e]` for completed state
    - VertraegeKachel uses `h-[88px]` (UI-SPEC §1 height)
    - FilterPillRow contains literal strings "Empfohlen", "Günstigste", "Beliebteste"
    - FilterPillRow active pill class includes `bg-primary text-white`
    - ProviderCard shows price with `text-[28px] font-bold` and "/Monat" unit at 16px
    - ProviderCard CTA button text is exactly "Jetzt abschließen →"
    - ProviderCard renders Empfohlen and Partnerangebot badges as mutually exclusive (only one absolute element top-right)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>3 components compile, match UI-SPEC values for spacing/typography/colors, ProviderCard receives `onAbschliessen` callback (wired in plan 03).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace Verträge Hub placeholder + build 3 simple comparison screens (Strom, Internet, Telefon)</name>
  <files>src/app/(main)/vertraege/page.tsx, src/app/(main)/vertraege/strom/page.tsx, src/app/(main)/vertraege/internet/page.tsx, src/app/(main)/vertraege/telefon/page.tsx</files>
  <read_first>
    - .planning/phases/03-vertraege-entdecken/03-UI-SPEC.md (sections 1, 2, 4, 5)
    - .planning/phases/03-vertraege-entdecken/03-PATTERNS.md (page-shell, sub-screen-header, sticky-top-bar sections)
    - src/app/(main)/vertraege/page.tsx (existing placeholder — to be replaced)
    - src/app/(main)/home/kostenrechner/page.tsx (back-button header analog)
    - src/app/(main)/anleitungen/page.tsx (page shell analog)
    - src/store/onboardingStore.ts (PLZ access)
  </read_first>
  <action>
**File 1: src/app/(main)/vertraege/page.tsx (REPLACE existing placeholder)**

`'use client'`. Use `useVertraegeStore`. Page shell `<div className="flex h-dvh flex-col bg-background"><div className="flex-1 overflow-y-auto px-4 pt-6 pb-4">`.

Header H1: `<h1 className="text-[20px] font-bold leading-[1.3] text-foreground">Verträge</h1>` (mt-0).

Intro card (mt-4):
```
<div className="flex items-start gap-2 rounded-[12px] border border-[#d2d5fc] bg-white p-4">
  <Info size={16} className="mt-0.5 shrink-0 text-muted-foreground" />
  <p className="text-[14px] font-normal text-muted-foreground">Schließ deine Verträge direkt hier ab. Wir erhalten eine kleine Provision — für dich entstehen keine Mehrkosten. ✓</p>
</div>
```

Progress row (mt-6): `<p className="text-[14px] font-bold text-foreground"><span className="text-primary">{count}</span> von 4 Verträgen abgeschlossen</p>` where `count = useVertraegeStore((s) => s.completedCount())`.

2x2 grid (mt-4): `grid grid-cols-2 gap-3`. Render 4 `<VertraegeKachel>`:
- kategorie='strom', emoji='⚡', label='Strom'
- kategorie='internet', emoji='🌐', label='Internet'
- kategorie='telefon', emoji='📱', label='Telefon'
- kategorie='versicherungen', emoji='🛡️', label='Versicherungen'

Pass `completed={completed[k]}` from the store.

**Files 2-4: src/app/(main)/vertraege/{strom,internet,telefon}/page.tsx**

Identical structure per file. Read the `kategorie` constant (one per file: 'strom', 'internet', 'telefon').

`'use client'`. Imports: `useRouter` from next/navigation, `ChevronLeft` from lucide-react, `getAnbieterByKategorie`, `KATEGORIE_META` from `@/lib/anbieter`, `useOnboardingStore`, `useState`, `ProviderCard`, `FilterPillRow`, `AbschlussSheet` (placeholder import — wired in plan 03).

State: `const [filter, setFilter] = useState<'empfohlen'|'guenstigste'|'beliebteste'>('empfohlen')`. (Sorting is visual-only in prototype; do not actually sort the list — the spec does not require it.)

Plan 03 will introduce AbschlussSheet wiring; for now, expose a stub `onAbschliessen` that does nothing (`() => {}`) so the page compiles independently. **Add comment**: `// AbschlussSheet wiring added in plan 03-03`.

Page shell: `<div className="flex h-dvh flex-col bg-background"><div className="flex-1 overflow-y-auto pb-4">`.

Sticky header block (sticky top-0 z-40 bg-background px-4 pt-4 pb-2):
- Back button row (flex items-center gap-2):
  - `<button onClick={() => router.push('/vertraege')} aria-label="Zurück" className="flex h-8 w-8 items-center justify-center"><ChevronLeft size={24} color="#1c2642"/></button>`
  - H1: `<h1 className="flex-1 text-[20px] font-bold text-foreground">{KATEGORIE_META[kategorie].headerTitle}</h1>`
  - spacer `<div className="h-8 w-8" />`
- Subline (mt-1, ml-10): `<p className="text-[14px] font-normal text-muted-foreground">PLZ {targetPlz} · {KATEGORIE_META[kategorie].sublineCount}</p>` (read `targetPlz` from `useOnboardingStore((s)=>s.data.targetPlz)`)
- FilterPillRow mt-3

Card list (px-4 mt-4 flex flex-col gap-3): `getAnbieterByKategorie(kategorie).map(a => <ProviderCard key={a.id} anbieter={a} onAbschliessen={() => {}} />)`.

For Versicherungen-equivalent file: this task does NOT cover versicherungen — that goes in Task 3.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "VertraegeKachel" src/app/\(main\)/vertraege/page.tsx && grep -c "ProviderCard" src/app/\(main\)/vertraege/strom/page.tsx && grep -c "targetPlz" src/app/\(main\)/vertraege/strom/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(main)/vertraege/page.tsx` no longer contains "Placeholder" or "TODO"; contains exactly 4 `<VertraegeKachel` invocations
    - Hub page contains the exact intro string "Schließ deine Verträge direkt hier ab. Wir erhalten eine kleine Provision — für dich entstehen keine Mehrkosten. ✓"
    - Hub page imports `useVertraegeStore`
    - Each of strom/internet/telefon page.tsx imports `ProviderCard` and `getAnbieterByKategorie`
    - Each comparison page reads `targetPlz` from `useOnboardingStore`
    - Each comparison page renders FilterPillRow
    - Each comparison page back button navigates to `/vertraege` (router.push)
    - `npx tsc --noEmit` passes
    - `npx next build` succeeds for all 4 routes
  </acceptance_criteria>
  <done>Hub replaces placeholder and shows 4 kacheln driven by store; 3 comparison pages render 4 provider cards each with sticky header + filter pills + PLZ subline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build Versicherungen comparison screen with Haftpflicht/Hausrat tabs</name>
  <files>src/app/(main)/vertraege/versicherungen/page.tsx</files>
  <read_first>
    - .planning/phases/03-vertraege-entdecken/03-UI-SPEC.md (section 6)
    - .planning/phases/03-vertraege-entdecken/03-CONTEXT.md (D-01, D-02)
    - src/app/(main)/aufgaben/uebergabeprotokoll/page.tsx (Base UI Tabs analog — `data-active:*` selectors)
    - src/components/ui/tabs.tsx
  </read_first>
  <action>
Create `src/app/(main)/vertraege/versicherungen/page.tsx`. `'use client'`.

Same shell + sticky back-button header + PLZ subline + FilterPillRow as Strom/Internet/Telefon (re-create — do not extract to shared component this plan; that's a refactor for later).

Header title: "Versicherungen vergleichen". Subline: `PLZ {targetPlz} · ~20 Anbieter`.

Below FilterPillRow, add Tabs:

```typescript
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';
const [tab, setTab] = useState<'haftpflicht'|'hausrat'>('haftpflicht');
```

Tabs JSX:
```
<Tabs value={tab} onValueChange={(v) => setTab(v as 'haftpflicht'|'hausrat')} className="mt-4">
  <TabsList className="flex h-11 w-full border-b border-[#d2d5fc] bg-white">
    <TabsTrigger value="haftpflicht"
      className="h-11 flex-1 px-3 text-[14px] font-bold text-muted-foreground data-active:text-primary data-active:shadow-[inset_0_-2px_0_0_#646efb]">
      Haftpflicht
    </TabsTrigger>
    <TabsTrigger value="hausrat"
      className="h-11 flex-1 px-3 text-[14px] font-bold text-muted-foreground data-active:text-primary data-active:shadow-[inset_0_-2px_0_0_#646efb]">
      Hausrat
    </TabsTrigger>
  </TabsList>
  <TabsContent value="haftpflicht" className="mt-4 flex flex-col gap-3">
    {getAnbieterByKategorie('haftpflicht').map(a => <ProviderCard key={a.id} anbieter={a} onAbschliessen={() => {}} />)}
  </TabsContent>
  <TabsContent value="hausrat" className="mt-4 flex flex-col gap-3">
    {getAnbieterByKategorie('hausrat').map(a => <ProviderCard key={a.id} anbieter={a} onAbschliessen={() => {}} />)}
  </TabsContent>
</Tabs>
```

Per D-01: Hub kachel for "versicherungen" navigates to this single screen (not directly to a sub-tab). Default tab is Haftpflicht.

Use `data-active:*` (Base UI), NOT `data-[state=active]:*` (Radix).

Note re completion: the Versicherungen kachel is marked complete via `markComplete('versicherungen')` regardless of which tab the user submitted from — that wiring lives in plan 03.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "data-active:" src/app/\(main\)/vertraege/versicherungen/page.tsx && grep -c "data-\[state=active\]" src/app/\(main\)/vertraege/versicherungen/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal strings "Haftpflicht" and "Hausrat" as TabsTrigger values
    - File uses `data-active:` selectors (grep -c returns >= 4 — twice per trigger for color + shadow)
    - File does NOT use `data-[state=active]` (grep -c returns 0)
    - File contains both `getAnbieterByKategorie('haftpflicht')` and `getAnbieterByKategorie('hausrat')`
    - Default tab state is `'haftpflicht'`
    - Header title is exactly "Versicherungen vergleichen"
    - `npx tsc --noEmit` passes
    - `npx next build` includes `/vertraege/versicherungen` route
  </acceptance_criteria>
  <done>Versicherungen route renders sticky header + filter pills + Haftpflicht/Hausrat tabs with 4 provider cards each, using Base UI selectors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mock affiliate URL → external redirect | ProviderCard CTA — currently `onAbschliessen={() => {}}` stub; plan 03 wires AbschlussSheet (no real redirect) |
| User input on filter pills | Local state only, no input field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Open redirect via affiliate links | mitigate | Per global threat: Provider CTA does NOT use real `<a href>` — it's a button with `onClick`. No href prevents URL injection |
| T-03-05 | Information disclosure | XSS via highlight tags | mitigate | All highlight strings come from hardcoded `ANBIETER` array (plan 03-01); React auto-escapes by default; no `dangerouslySetInnerHTML` used |
| T-03-06 | Spoofing | Fake "Empfohlen" badge | accept | Prototype scope; provider list and badges are mock data; no commercial relationship implied |
</threat_model>

<verification>
- `npx next build` succeeds with all 5 new routes (/vertraege, /vertraege/strom, /vertraege/internet, /vertraege/telefon, /vertraege/versicherungen)
- Visual check on 375px viewport: 2x2 hub grid renders, comparison screens scroll, sticky header stays at top, filter pills visible, 4 provider cards per screen
- Versicherungen tab switching shows different provider lists
</verification>

<success_criteria>
- ROADMAP Phase 3 success criterion 1 (Hub shows 4 kacheln + progress) — met
- ROADMAP Phase 3 success criterion 2 (Strom comparison shows ≥4 cards with badges) — met
- ROADMAP Phase 3 success criterion 4 (all 4 categories have comparison screens) — met
- All 3 components are reusable; ProviderCard exposes `onAbschliessen` for plan 03 wiring
</success_criteria>

<output>
After completion, create `.planning/phases/03-vertraege-entdecken/03-02-SUMMARY.md` documenting: components shipped, routes created, filter sorting implementation note (visual-only), Versicherungen completion mapping (single hub key for both tabs), any deviations from UI-SPEC.
</output>
