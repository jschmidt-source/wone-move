---
phase: 2
slug: home-aufgaben
status: approved
reviewed_at: 2026-05-08
shadcn_initialized: true
preset: base-nova
created: 2026-05-08
---

# Phase 2 — UI Design Contract: Home + Aufgaben (Checkliste & Zeitplan)

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (initialized — `components.json` confirmed, style: base-nova) |
| Preset | base-nova (Radix/Nova) |
| Component library | Radix UI (via shadcn) |
| Icon library | Lucide React (bundled with shadcn) |
| Font | Plus Jakarta Sans — loaded via `next/font/google`, wired as `--font-plus-jakarta-sans` in globals.css |

**Carries forward from Phase 1:** All tokens in `src/app/globals.css` @theme block. No re-initialization required.

**New shadcn components needed this phase:** `progress`, `slider`, `tabs`, `collapsible`, `scroll-area` — all from shadcn official registry.

**Source:** components.json (confirmed), globals.css (confirmed), 01-UI-SPEC.md.

---

## Spacing Scale

Unchanged from Phase 1. Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge inner padding, category dot spacing, time-badge padding |
| sm | 8px | Checklist item internal gap (checkbox to label), card internal vertical rhythm, collapsible header gap |
| md | 16px | Default horizontal screen padding, card inner padding, section header margin, form field gap |
| lg | 24px | Section vertical gap (between Dashboard cards), between checklist category blocks |
| xl | 32px | Dashboard top welcome block bottom margin |
| 2xl | 48px | Not used in Phase 2 |
| 3xl | 64px | Bottom navigation safe-area (carried from Phase 1) |

**Phase 2 exceptions (with justifications):**
- Checklist item row height: 56px minimum — touch target safety (same rule as Phase 1 toggle rows; 56px is the smallest comfortable tap area for list items at mobile density)
- Progress bar height: 8px — specific component dimension, not a layout spacing value; this is a visual track, not a gap between elements
- Countdown number display: 40px font size — Display-size exception for numeric hero element (see Typography section)
- Horizontal scroll strip (Wusstest du schon?): cards 200px wide, 12px gap between cards — 12px used instead of 8px or 16px because the strip is a compact horizontal scroller where 8px feels too tight and 16px wastes the visible peek of the next card; 12px is the tightest value that preserves readable card separation at 200px card width
- Zimmer-Tabs touch area: 44px minimum height — iOS/Android minimum recommended tap target (44×44px per HIG / Material); tab bars are high-frequency touch targets
- Kostenrechner slider thumb: 24px diameter (Radix Slider default, touch-safe) — matches Radix UI default; overriding would require custom CSS with no UX benefit
- Quick-Action tile height: 72px — 3-column layout with centered icon + label; 64px clips the label at 14px with 4px gap; 72px provides 8px breathing room above icon and below label within the column constraint
- Input and CTA height: 52px — slightly above standard 48px to give input fields and primary CTAs visual weight commensurate with their importance on mobile; also matches shadcn default for comfortable thumb targets
- FAB clearance: 80px above bottom nav top edge — ensures the last list item is never obscured by both the FAB (56px) and its shadow (12px spread); `padding-bottom: 80px` on content area is the computed clearance value

**Source:** Phase 1 UI-SPEC.md spacing scale; Phase 2 exceptions derived from ROADMAP.md screen specs.

---

## Typography

Unchanged from Phase 1. Four sizes, two weights:

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 16px | 400 (Regular) | 1.5 | Dashboard body text, checklist item labels, guide step descriptions, Kostenrechner labels |
| Label | 14px | 700 (Bold) | 1.4 | Time badges ("~15 Min"), category labels, Zeitplan bucket headers, tab labels, input labels, badge text, condition toggle button labels, photo slot labels, signature labels, Must-Do badge text, date pills on deadline rows, meta badges on guide detail, min/max slider labels, category label in Anleitungen cards, Premium badge |
| Heading | 20px | 700 (Bold) | 1.3 | Dashboard section headers ("Nächste Aufgabe"), checklist category titles, guide screen title, Übergabeprotokoll section headings, Zeitplan bucket headers |
| Display | 28px | 700 (Bold) | 1.2 | Dashboard greeting headline ("Hey Lea") |

**Countdown number exception:** 40px, weight 700, line-height 1.0 — single decorative numeric callout on the Dashboard Welcome block, non-content role, single location. Not a typographic scale member. Rationale: the countdown number is a standalone hero numeral (e.g. "12") placed adjacent to contextual subline copy; its purpose is visual impact, not reading flow. Equivalent to a stat callout in a marketing card. Declared explicitly here so the executor treats it as an intentional exception, not a bug.

**12px is not used anywhere in Phase 2.** All elements previously considered at 12px (badge text, condition toggle button labels, photo slot labels, signature labels, Must-Do badge text, time badges, date pills, step number numerals, min/max slider labels) use the **Label role (14px)**. The 2px difference is imperceptible at mobile density on these small elements and avoids introducing a fifth size. This mirrors the Phase 1 decision to unify nav tab labels at 14px rather than spec a separate 10px or 11px size.

**Source:** 01-UI-SPEC.md typography table (locked). Countdown exception derived from ROADMAP.md Dashboard screen spec for countdown prominence.

---

## Color

All tokens carried from Phase 1 globals.css. No new tokens introduced.

| Role | Hex | CSS Variable | Usage |
|------|-----|-------------|-------|
| Dominant (60%) | `#f6f7f7` | `--background` | Page backgrounds, screen surfaces, checklist row backgrounds |
| Secondary (30%) | `#1c2642` | `--foreground` | Body text, headings, checklist item text, guide copy |
| Accent (10%) | `#646efb` | `--primary` | Reserved elements — see list below |
| Accent light | `#d2d5fc` | `--secondary` / `--border` | Must-Do badge background, selected tab underline bg, progress bar track, category dot border |
| Muted text | `#5b6377` | `--muted-foreground` | Time estimates ("~15 Min"), category counts, Zeitplan secondary labels, Kostenrechner result secondary labels |
| Card surface | `#ffffff` | `--card` | All card backgrounds (Dashboard cards, checklist category headers, guide cards, Übergabeprotokoll room panels) |
| Destructive | `#ef4444` | `--destructive` | Must-Do badge foreground (red badge text/border), Zeitplan "überfällig" item color |

**Accent (`#646efb`) is reserved for:**
1. Progress bar filled segment (PROG-01)
2. Checkliste ↔ Zeitplan toggle active state
3. Checked checkbox fill color (checked items)
4. "Jetzt erledigen →" CTA on Nächste Aufgabe card
5. "Als erledigt markieren ✓" CTA on Guide detail screen
6. City-Link "Zum Bürgeramt München →" (highlighted external link)
7. Active Zimmer-Tab underline / border indicator
8. Kostenrechner slider thumb and filled track
9. "Eigenen Punkt hinzufügen" floating button background

**Semantic colors for new states:**
- Zeitplan "erledigt" item: `#22c55e` (Tailwind green-500) — distinct from accent; not in brand palette but required for semantic clarity
- Zeitplan "diese Woche" / "fällig" item: `#f97316` (Tailwind orange-500) — urgency signal
- Zeitplan "überfällig" item: `#ef4444` (`--destructive`) — danger signal
- Must-Do badge: `#ef4444` text on `#fef2f2` background (red-50 equivalent)
- Premium badge: `#f97316` text on `#fff7ed` background (orange-50 equivalent) — "Protokoll exportieren"

**60/30/10 split:**
- 60% `#f6f7f7` — page backgrounds, scroll areas, category collapsed backgrounds
- 30% `#1c2642` — all body text, headings, nav text, guide copy
- 10% `#646efb` — CTAs, active toggle, progress fill, checked states

**Source:** 01-UI-SPEC.md color table (locked). Semantic timeline colors and Must-Do badge color from ROADMAP.md Phase 2 spec ("grün = erledigt, orange = diese Woche, rot = überfällig").

---

## Screen Inventory & Interaction Contracts

### 1. Home / Dashboard (`/home`)

**Layout:** Scrollable single-column, 16px horizontal padding, content starts 16px below status bar, 24px gap between sections.

**Welcome block:**
- Greeting: "Hey Lea 👋" — 28px weight 700 `#1c2642`
- Countdown subline: "Noch [X] Tage bis zu deinem Umzug" — 16px weight 400 `#5b6377`
- Countdown number: standalone display of X — 40px weight 700 `#646efb` — rendered as a numeric callout adjacent to subline text
- Top padding from safe-area: 24px

**Fortschrittsbalken (PROG-01):**
- shadcn `progress` component
- Track height: 8px, border-radius: 4px, track color `#d2d5fc`, filled color `#646efb`
- Percentage label: 14px weight 700 `#1c2642` — right-aligned above bar
- Section label: "Dein Fortschritt" — 14px weight 700 `#5b6377` — left-aligned above bar
- 16px gap between label row and bar

**Nächste Aufgabe card:**
- shadcn `card` component — white background, border-radius 14px, 16px inner padding, border 1px `#d2d5fc`
- Card header: "Nächste Aufgabe" — 14px weight 700 `#5b6377`
- Task title: 16px weight 700 `#1c2642`
- Time badge: "~X Min" — 14px weight 700, `#5b6377` text on `#f6f7f7` pill, 4px vertical / 8px horizontal padding, border-radius 99px
- CTA: "Jetzt erledigen →" — 14px weight 700 `#646efb`, text-only link style, no button border
- Full card is tappable → navigates to `/anleitungen/ummeldung` (or relevant slug)

**Wusstest du schon? card strip:**
- Section label: "Wusstest du schon?" — 20px weight 700 `#1c2642`
- Horizontal scroll container — `overflow-x: scroll`, `scroll-snap-type: x mandatory`, `-webkit-overflow-scrolling: touch`
- 3 tip cards, each: width 200px, height 120px, white background, border-radius 12px, border 1px `#d2d5fc`, 16px padding, `scroll-snap-align: start`
- Gap between cards: 12px
- First card left-aligned with 16px screen edge; strip itself has no additional padding (cards handle their own margin)
- Card content: tip text 14px weight 400 `#1c2642`, up to 3 lines; no images
- No scroll indicator dots — the partial right-edge peek of the 3rd card signals scrollability

**Quick-Actions row:**
- Section label: "Schnellzugriff" — 14px weight 700 `#5b6377`
- 3 tiles in a row, equal width (screen width − 32px) / 3 − 8px gap
- Tile: white background, border-radius 12px, border 1px `#d2d5fc`, height 72px, centered icon + label below
- Icon: Lucide, 20px, `#646efb`
- Label: 14px weight 700 `#1c2642`, centered below icon, 4px gap
- Tiles: "Verträge" (FileText icon), "Zeitplan" (CalendarDays icon), "Kostenrechner" (Calculator icon)
- Tap → navigates to respective route

**Upcoming Deadlines strip:**
- Section label: "Anstehende Fristen" — 14px weight 700 `#5b6377`
- 2–3 deadline rows, each: horizontal layout, date pill left, task name right
- Date pill: 14px weight 700, color-coded — orange for "diese Woche", red for "überfällig"
- Task name: 14px weight 400 `#1c2642`
- Row height: 44px, separated by `#d2d5fc` 1px divider
- Mock data: 2–3 items with hardcoded dates offset from moveDate

---

### 2. Kostenrechner (`/home/kostenrechner`)

**Layout:** Scrollable, 16px padding, back button top-left.

**Header:**
- Back button: Lucide `ChevronLeft`, 24px, `#1c2642`, tap → `router.back()`
- Screen title: "Kostenrechner" — 20px weight 700 `#1c2642`, centered in header row

**Slider — Entfernung:**
- Label: "Entfernung" — 14px weight 700 `#1c2642`
- Value display: "[X] km" — 16px weight 700 `#646efb`, right of label
- shadcn `slider` component — range 10–500, step 10, thumb `#646efb`, filled track `#646efb`, unfilled track `#d2d5fc`, track height 4px, thumb diameter 24px
- Min/max labels: "10 km" / "500 km" — 14px weight 400 `#5b6377` below slider

**Toggle — Helfer:**
- Label: "Helfer vorhanden?" — 14px weight 700 `#1c2642`
- 3-option segmented control (not shadcn Switch — use custom pill row):
  - 3 buttons side by side, equal width, height 40px, border-radius 8px
  - Options: "Ja", "Nein", "Umzugsfirma"
  - Default: "Nein" selected
  - Selected: `#646efb` background, white text 14px weight 700
  - Unselected: white background, `#d2d5fc` border, 14px weight 700 `#1c2642`
  - Full-width group with 4px gap between buttons

**Input — Wohnungsgröße:**
- Label: "Wohnungsgröße" — 14px weight 700 `#1c2642`
- shadcn `input` — height 52px, border 1.5px `#d2d5fc` at rest → `#646efb` on focus, border-radius 10px, 16px inner padding, inputmode="numeric"
- Placeholder: "z.B. 42" — `#5b6377`
- Unit label "qm" — 14px weight 400 `#5b6377` appended inside input right side

**Ergebnis-Card:**
- shadcn `card` — white, border-radius 14px, 16px padding, border 1px `#d2d5fc`
- Card title: "Geschätzte Kosten" — 14px weight 700 `#5b6377`
- 4 line items (label left, value right):
  - "Kartons" / "Transporter" / "Sonstiges" / "**Gesamt**"
  - Line item font: 14px weight 400 `#1c2642`; value: 14px weight 700 `#1c2642`
  - Gesamt row: 16px weight 700 `#1c2642` both sides, separated above by 1px `#d2d5fc` divider, 8px vertical gap
  - Gesamt value shows range: "€X–€X"
- Card appears immediately (reactive to slider/toggle state changes) — no "Berechnen" button needed

---

### 3. Aufgaben — Checkliste-Ansicht (`/aufgaben`)

**Layout:** Scrollable, 16px horizontal padding. View toggle pinned at top (sticky below status bar).

**View Toggle (sticky, 48px height):**
- Two-option segmented control: "Checkliste" | "Zeitplan"
- Width: 100% of content area
- Height: 36px, border-radius 8px, contained in white 48px bar with 8px top/bottom padding
- Active pill: `#646efb` background, white 14px weight 700
- Inactive: transparent background, `#5b6377` 14px weight 700
- Background bar: white, bottom border 1px `#d2d5fc`
- Sticky via `position: sticky; top: 0; z-index: 40`

**Must-Do section (pinned, above categories):**
- Section header: "Muss erledigt werden" — 14px weight 700 — left; "Must-Do" badge right: red background `#ef4444`, white text 14px weight 700, border-radius 99px, 4px vertical / 8px horizontal padding
- 3–5 items (Ummeldung, Stromanbieter wählen, Haftpflichtversicherung abschließen)
- Each item: see Checklist Item spec below
- Section background: white card, border-radius 12px, 1px border `#fecaca` (red-200)

**Checklist Item spec (used in Must-Do and all categories):**
- Row height: 56px minimum (touch target)
- Layout: [checkbox 24px] [8px gap] [title + time badge column, flex-1] [optional arrow 20px]
- Checkbox: custom component — 20px square, border-radius 4px
  - Unchecked: border 2px `#d2d5fc`, white fill
  - Checked: `#646efb` fill, white checkmark SVG inside, scale(1.1) + color transition 150ms ease
  - Tapping a checked item unchecks it (toggle)
- Title: 16px weight 400 `#1c2642`; when checked: `text-decoration: line-through`, color `#5b6377`
- Time badge: "~X Min" — 14px weight 700, `#5b6377` on `#f6f7f7` pill, 4px/8px padding, border-radius 99px — below title with 2px gap
- Category dot: 8px diameter circle, category color (see below), left of time badge with 4px gap
- Arrow (if tappable to guide): Lucide `ChevronRight`, 16px, `#5b6377` — only on items that have a guide

**Category color dots:**
| Category | Color |
|----------|-------|
| Organisatorisches | `#646efb` (brand primary) |
| Verträge | `#06b6d4` (cyan-500) |
| Versicherungen | `#8b5cf6` (violet-500) |
| Einrichtung | `#f59e0b` (amber-500) |
| Finanzen | `#22c55e` (green-500) |

**Collapsible Categories (5):**
- Each category wrapped in a collapsible block
- Collapsed header: category name (16px weight 700 `#1c2642`) + color dot (8px) + completion count "X/Y" (14px weight 400 `#5b6377`) + Lucide `ChevronDown` 20px `#5b6377` right
- Expanded: header + item list below; `ChevronDown` rotates 180° (CSS transition 200ms ease)
- Category header row height: 52px
- Category background (collapsed): white, border-radius 12px, border 1px `#d2d5fc`
- Category background (expanded): white, border-radius 12px top only, items below in same block with 1px `#d2d5fc` left border indent (8px left margin on items)
- 12px gap between category blocks

**Eigenen Punkt hinzufügen:**
- Floating action button (FAB): bottom right, 56px × 56px, background `#646efb`, border-radius 50%, shadow `0 4px 12px rgba(100, 110, 251, 0.3)`, `z-index: 30`
- Icon: Lucide `Plus`, 24px white
- `aria-label="Eigenen Punkt hinzufügen"` — required for screen reader accessibility
- Positioned 16px from right edge, 80px above bottom nav top edge (clear of nav)
- Tap → opens a bottom sheet (see interaction below)
- **Add item bottom sheet:**
  - Slide-up from bottom, height 240px, border-radius 16px 16px 0 0, white background
  - Handle: 4px × 32px, `#d2d5fc`, centered, 12px from top
  - Title: "Eigenen Punkt hinzufügen" — 16px weight 700 `#1c2642`, 24px from handle
  - Input: shadcn `input`, full-width, 16px padding, placeholder "z.B. Nachsendeauftrag einrichten"
  - Category selector: 5 small pills with category dots (same colors as above), horizontal scroll, tap to select — selected has `#646efb` border, unselected has `#d2d5fc` border
  - CTA: "Punkt hinzufügen" — full-width, 52px, `#646efb`, white text, border-radius 12px
  - Dismiss: tap outside or drag down

**Check animation (D-03 — CSS only, no confetti per item):**
- On check: checkbox transitions background from white → `#646efb` in 150ms with `scale(1.1)` keyframe
- Title immediately applies `line-through` + color change to `#5b6377` in 150ms
- No bouncing, no particle effects — contained to the checkbox element

---

### 4. Aufgaben — Zeitplan-Ansicht (toggle from Checkliste)

**Layout:** Same scrollable container, same sticky toggle, same 16px padding. Content replaces checklist view on toggle.

**5 Time Bucket sections:**
- Bucket header: bucket label (20px weight 700 `#1c2642`) + item count badge (14px weight 700, `#5b6377` background `#f6f7f7`, pill) — left/right row, 52px height
- 16px gap between buckets

**Buckets:**
| Label | Key |
|-------|-----|
| "4+ Wochen vorher" | far |
| "2–4 Wochen vorher" | medium |
| "1 Woche vorher" | soon |
| "Am Umzugstag" | moving-day |
| "Danach erledigen" | after |

**Time Bucket item spec:**
- Same row structure as Checklist Item (checkbox + title + badge)
- **Color-coding applied to the left border accent** (4px left border on row) and category dot:
  - `erledigt` (checked): left border `#22c55e` (green-500)
  - `diese Woche` (within 7 days of today): left border `#f97316` (orange-500)
  - `überfällig` (past due date): left border `#ef4444` (destructive)
  - `normal` (future): left border `#d2d5fc`
- Title text color unchanged from checklist spec (color-coding via border only — text remains readable)
- Checking an item in Zeitplan view also updates checklistStore (same state, same source of truth)

---

### 5. Aufgabe-Detail / Anleitung — Ummeldung (`/anleitungen/ummeldung`)

**Layout:** Full-screen scroll, 16px padding, no bottom nav visible on this screen (full-screen overlay pattern).

**Wait — correction:** Bottom nav IS visible (this is a tab child route via `(main)` layout). Back navigation via `router.back()` button in screen header (D-08).

**Header row:**
- Back button: Lucide `ChevronLeft` 24px `#1c2642` left; tap → `router.back()`
- Screen title: "Ummeldung beim Bürgeramt" — 20px weight 700 `#1c2642` — truncated to 1 line with ellipsis if needed

**Meta row (below header):**
- Three badges in a horizontal row: category tag ("Organisatorisches"), time ("~15 Min"), difficulty ("Mittel")
- Badge: 14px weight 700, `#5b6377` text, `#f6f7f7` background, 4px/8px padding, border-radius 99px, 4px gap between badges
- Category badge uses category color dot prepended (8px dot)

**Dokumente-Box:**
- shadcn `card` — white, border-radius 12px, 16px padding, border-left 4px solid `#646efb`
- Header: Lucide `FileText` 16px `#646efb` + "Benötigte Dokumente" 14px weight 700 `#1c2642`, 8px gap
- Document list: 3 items, each 14px weight 400 `#1c2642`, 4px gap, bullet via `•` character in `#646efb`
- Items: "Personalausweis", "Wohnungsgeberbestätigung (Mieterbescheinigung)", "ggf. Reisepass"

**Step list:**
- 4 steps, numbered
- Step number: 24px × 24px circle, `#646efb` background, white 14px weight 700, flex-shrink 0
- Step title: 16px weight 700 `#1c2642`
- Step description: 14px weight 400 `#5b6377`, below title, 4px gap
- 16px gap between steps
- Steps connected by a 1px vertical dashed line `#d2d5fc` from circle bottom to next circle top

**Wichtiger Hinweis (warning box):**
- Background `#fff7ed` (orange-50), border-left 4px solid `#f97316`, border-radius 8px, 16px padding
- Icon: Lucide `AlertTriangle` 16px `#f97316` left
- Copy: "Frist: 14 Tage nach Einzug!" — 14px weight 700 `#f97316`

**City-Link:**
- Styled as a tappable card row: white, border-radius 12px, border 1px `#d2d5fc`, 16px padding, 52px height
- Icon: Lucide `MapPin` 20px `#646efb` left
- Label: "Zum Bürgeramt München →" — 14px weight 700 `#646efb`
- Mock link (no actual navigation, `href="#"` or disabled)

**Bottom CTAs:**
- Primary: "Als erledigt markieren ✓" — full-width, 52px, `#646efb`, white 16px weight 700, border-radius 12px
- Secondary: "← Zurück" — full-width, 52px, white background, `#1c2642` text 16px weight 700, border 1.5px `#d2d5fc`, border-radius 12px, 8px gap above primary
- Both pinned to bottom of scroll content (not fixed — scroll down to reach them)

---

### 6. Übergabeprotokoll (`/aufgaben/uebergabeprotokoll`)

**Layout:** Full-screen, bottom nav visible. Scrollable below tabs.

**Header:** "Übergabeprotokoll" — 20px weight 700, centered. Back button left (ChevronLeft).

**Intro text:** "Dokumentiere den Zustand deiner neuen Wohnung beim Einzug." — 14px weight 400 `#5b6377`, 16px padding, 8px below header.

**Zimmer-Tabs (shadcn `tabs` component):**
- 5 tabs: Wohnzimmer | Küche | Bad | Schlafzimmer | Flur
- Tab bar: horizontal scroll (overflow-x scroll) if tabs exceed screen width — no clipping
- Tab: 14px weight 700, height 44px
- Active tab: `#646efb` text, 2px underline `#646efb`
- Inactive tab: `#5b6377` text, no underline
- Tab bar background: white, bottom border 1px `#d2d5fc`

**Per-Zimmer content:**
- 5 condition fields: Wände, Boden, Fenster, Türen, Beleuchtung
- Each field row: label (16px weight 700 `#1c2642`) left, condition toggle right
- Row height: 56px, bottom border 1px `#d2d5fc` except last row
- Condition toggle (3-state, custom — NOT shadcn Switch):
  - "Gut" button: 68px × 32px, border-radius 8px; selected: `#22c55e` background, white 14px weight 700; unselected: `#f6f7f7` border 1px `#d2d5fc` 14px `#5b6377`
  - "Mangel" button: same size; selected: `#ef4444` background, white 14px weight 700; unselected: same rest state
  - 4px gap between buttons
- When "Mangel" selected: Freitext input slides in below row (animate height 0 → 48px, 200ms ease):
  - shadcn `input`, placeholder "Mangel beschreiben...", full-width, 14px font

**Foto-Slots:**
- 3 photo slot placeholders per room, arranged in a 3-column grid
- Each slot: square (width/3 − 8px, aspect ratio 1:1), border 1.5px dashed `#d2d5fc`, border-radius 8px, background `#f6f7f7`
- Content: Lucide `Camera` 20px `#5b6377` centered + "Foto" 14px `#5b6377` below
- Tap → no action in prototype (mock)

**Unterschriften-Bereich:**
- 2 signature blocks side-by-side: "Mieter" + "Vermieter"
- Each: bordered rectangle 120px × 60px, border 1.5px dashed `#d2d5fc`, border-radius 8px, label 14px weight 400 `#5b6377` centered
- Mock — not interactive

**Protokoll exportieren row:**
- Full-width tappable row: white card, border-radius 12px, border 1px `#d2d5fc`, 16px padding, 52px height
- Icon: Lucide `Download` 20px `#5b6377` left
- Label: "Protokoll exportieren" — 14px weight 700 `#1c2642`
- Premium badge right: "Premium" — 14px weight 700, `#f97316` text on `#fff7ed` background, border-radius 99px, 4px/8px padding
- Tap → no action (mock; premium gate shown in Phase 4)

---

### 7. Anleitungen-Browser (`/anleitungen`)

**Layout:** Scrollable grid, 16px padding.

**Header:** "Anleitungen" — 20px weight 700 `#1c2642`.

**Card grid:** 2-column grid, 12px gap
- Each card: shadcn `card` — white, border-radius 12px, border 1px `#d2d5fc`, 16px padding
- Card content:
  - Category color dot (8px) + category label (14px weight 700 `#5b6377`) — top-left row
  - Guide title: 16px weight 700 `#1c2642`, max 2 lines
  - Time + difficulty row: "~X Min" badge + "Leicht/Mittel/Schwer" badge — same pill style as detail screen meta
  - 8px gap between elements
- Card height: auto (content determines height)
- Tapping card → navigates to `/anleitungen/[slug]`

**Fully completed guides** (Ummeldung + 2 others): normal card appearance.

**Preview-only guides** (remaining): card has a subtle `opacity: 0.85` and no chevron indicator — same navigation, placeholder content on detail screen.

---

## Copywriting Contract

| Element | German Copy | Source |
|---------|-------------|--------|
| Dashboard greeting | "Hey Lea 👋" | ROADMAP.md D-10 (name hardcoded) |
| Dashboard countdown | "Noch [X] Tage bis zu deinem Umzug" | ROADMAP.md |
| Dashboard next task header | "Nächste Aufgabe" | ROADMAP.md |
| Dashboard next task CTA | "Jetzt erledigen →" | Default (imperative + arrow) |
| Dashboard tips section header | "Wusstest du schon?" | ROADMAP.md |
| Dashboard quick-actions header | "Schnellzugriff" | Default |
| Dashboard deadlines header | "Anstehende Fristen" | Default |
| Checklist toggle — option 1 | "Checkliste" | ROADMAP.md |
| Checklist toggle — option 2 | "Zeitplan" | ROADMAP.md |
| Must-Do section label | "Muss erledigt werden" | Default (descriptive, not alarming) |
| Must-Do badge | "Must-Do" | ROADMAP.md |
| Add custom item FAB tooltip | — | No tooltip — icon speaks for itself; accessibility via aria-label |
| Add item sheet title | "Eigenen Punkt hinzufügen" | ROADMAP.md CHK-04 |
| Add item CTA | "Punkt hinzufügen" | Imperative + noun pattern (updated from "Hinzufügen") |
| Add item placeholder | "z.B. Nachsendeauftrag einrichten" | Default |
| Guide detail back | "← Zurück" | ROADMAP.md |
| Guide detail mark-done CTA | "Als erledigt markieren ✓" | ROADMAP.md |
| Guide city-link | "Zum Bürgeramt [Stadt] →" | ROADMAP.md (city from targetPlz) |
| Guide warning | "Frist: 14 Tage nach Einzug!" | ROADMAP.md |
| Übergabeprotokoll intro | "Dokumentiere den Zustand deiner neuen Wohnung beim Einzug." | ROADMAP.md |
| Übergabeprotokoll export row | "Protokoll exportieren" | ROADMAP.md |
| Premium badge | "Premium" | ROADMAP.md |
| Kostenrechner header | "Kostenrechner" | ROADMAP.md |
| Kostenrechner distance label | "Entfernung" | Default |
| Kostenrechner helpers label | "Helfer vorhanden?" | ROADMAP.md |
| Kostenrechner area label | "Wohnungsgröße" | ROADMAP.md |
| Kostenrechner area placeholder | "z.B. 42" | Default |
| Kostenrechner result header | "Geschätzte Kosten" | ROADMAP.md |
| Kostenrechner result total label | "Gesamt" | Default |
| Zeitplan bucket 1 | "4+ Wochen vorher" | ROADMAP.md |
| Zeitplan bucket 2 | "2–4 Wochen vorher" | ROADMAP.md |
| Zeitplan bucket 3 | "1 Woche vorher" | ROADMAP.md |
| Zeitplan bucket 4 | "Am Umzugstag" | ROADMAP.md |
| Zeitplan bucket 5 | "Danach erledigen" | ROADMAP.md |
| Anleitungen browser header | "Anleitungen" | Default |
| Empty state — no tasks | "Alle Aufgaben erledigt! 🎉" | Default |
| Empty state — body | "Du hast alles auf deiner Liste abgehakt." | Default |
| Error — custom item empty | "Bitte gib einen Namen für den Punkt ein." | Default (inline below input) |
| Difficulty labels | "Leicht" / "Mittel" / "Schwer" | Default (German) |

**Destructive actions in Phase 2:**
- Unchecking a completed task — NOT treated as destructive. No confirmation required. State reverts silently (toggle behavior, per D-03).
- There are no delete actions in Phase 2 (custom items can be added but not deleted until Phase 4 polish).

**Source:** ROADMAP.md Phase 2 screen specs for locked copy. "Default" entries are derived from screen function and consistent German informal tone (du-form throughout).

---

## Animation & Motion Contract

Carries Phase 1 contract. New additions for Phase 2:

| Animation | Trigger | Duration | Easing | Implementation |
|-----------|---------|----------|--------|----------------|
| Splash → Welcome | Auto | 300ms | ease-out | Phase 1 (carried) |
| Onboarding step transition | "Weiter" | 250ms | ease-in-out | Phase 1 (carried) |
| CTA button press | Tap | 100ms scale 0.97 | ease | Phase 1 (carried) |
| Checklist toggle (Checkliste ↔ Zeitplan) | Tap | 200ms | ease-in-out | Fade-out content + fade-in new content (opacity 0→1); do NOT slide to avoid visual disorientation |
| Checkbox check | Tap | 150ms | ease | Scale 1→1.1→1 + background fill transition (CSS only, per D-03) |
| Checkbox line-through | On check | 150ms | ease | CSS text-decoration + color transition |
| Collapsible category open/close | Tap header | 200ms | ease-in-out | Height animation via CSS grid trick (`grid-template-rows: 0fr → 1fr`) |
| Add-item bottom sheet open | FAB tap | 250ms | ease-out | Translate Y from 100% to 0 |
| Add-item bottom sheet close | Tap outside / drag | 200ms | ease-in | Translate Y to 100% |
| Mangel freitext expand | Toggle "Mangel" | 200ms | ease | Height 0 → 48px |
| Kostenrechner result update | Slider/toggle change | 100ms | ease | Opacity pulse 0.7→1 on value text (signals recalculation) |
| Zeitplan bucket expand | Always expanded | — | — | No collapse in Zeitplan view; all 5 buckets visible simultaneously |

**No per-item confetti in Phase 2** — confirmed by D-03. Confetti reserved for Celebration screen (Phase 1) and full completion (Phase 4).

---

## Component Inventory

### Carried from Phase 1 (already installed)

| Component | shadcn Name | Phase 2 Usage |
|-----------|-------------|---------------|
| Button | `button` | All CTAs |
| Input | `input` | Add-item sheet, Kostenrechner qm field, Mangel freitext |
| Switch | `switch` | Not used in Phase 2 (replaced by segmented controls) |
| Card | `card` | Dashboard cards, Guide Dokumente box, Ergebnis card, Anleitungen grid cards |
| Badge | `badge` | Time badges, Must-Do badge, difficulty labels |

### New components needed for Phase 2

| Component | shadcn Name | Install Command | Used In |
|-----------|-------------|-----------------|---------|
| Progress | `progress` | `npx shadcn add progress` | Dashboard Fortschrittsbalken (PROG-01) |
| Slider | `slider` | `npx shadcn add slider` | Kostenrechner Entfernung |
| Tabs | `tabs` | `npx shadcn add tabs` | Übergabeprotokoll Zimmer-Tabs |
| Collapsible | `collapsible` | `npx shadcn add collapsible` | Checkliste category accordion |
| Scroll Area | `scroll-area` | `npx shadcn add scroll-area` | Wusstest-du-schon horizontal strip (optional — CSS overflow-x may suffice) |

### Custom components (no shadcn equivalent)

| Component | Location | Description |
|-----------|----------|-------------|
| ChecklistItem | `src/components/checklist/ChecklistItem.tsx` | Checkbox + title + time badge row |
| CategorySection | `src/components/checklist/CategorySection.tsx` | Collapsible category wrapper |
| MustDoSection | `src/components/checklist/MustDoSection.tsx` | Pinned Must-Do block |
| TimelineBucket | `src/components/timeline/TimelineBucket.tsx` | Zeitplan bucket section |
| GuideStepList | `src/components/guide/GuideStepList.tsx` | Numbered step list with connector line |
| SegmentedControl | `src/components/ui/SegmentedControl.tsx` | 2-or-3 option pill control (view toggle + Helfer toggle) |
| ConditionToggle | `src/components/uebergabe/ConditionToggle.tsx` | Gut/Mangel two-button toggle |
| PhotoSlot | `src/components/uebergabe/PhotoSlot.tsx` | Dashed placeholder camera slot |
| AddItemSheet | `src/components/checklist/AddItemSheet.tsx` | Bottom sheet for custom item creation |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | progress, slider, tabs, collapsible, scroll-area (+ Phase 1: button, input, card, badge) | not required — official registry |
| Third-party | none | not applicable |

---

## Layout Rules

Carries Phase 1 rules. Phase 2 additions:

- **Sticky toggle bar:** `position: sticky; top: 0; z-index: 40` — sticks below status bar, above scrolling content
- **FAB clearance:** floating action button sits 80px above bottom nav top edge to avoid overlap with last list item; content area has `padding-bottom: 80px` when FAB is present
- **Horizontal scroll strips:** only the Wusstest-du-schon strip and Zimmer-Tabs bar scroll horizontally; all other layouts remain single-column vertical scroll
- **Guide screen:** full-screen within `(main)` layout (bottom nav visible); content scrolls with CTAs at natural document flow end, not fixed-position
- **Collapsible sections:** all 5 categories start in collapsed state on first render; Must-Do section always expanded
- **Zeitplan:** all 5 time buckets always visible (no collapsing); user scrolls vertically through all buckets

---

## Pre-Population Audit

All sections of this spec were pre-populated from upstream artifacts. No questions were asked of the user.

| Source | Decisions Used |
|--------|---------------|
| CONTEXT.md (02-CONTEXT.md) | 12 decisions (D-01 through D-12) |
| ROADMAP.md Phase 2 | All screen specs, copy, success criteria |
| 01-UI-SPEC.md | Spacing scale, typography, color tokens, animation patterns |
| globals.css | Token values confirmed (--primary, --background, etc.) |
| components.json | shadcn initialized, style: base-nova, icon library: lucide |
| REQUIREMENTS.md | CHK-01–05, GUIDE-01–05, PROG-01–02, TIME-01–02 |
| User input | 0 (no questions needed) |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-05-08
